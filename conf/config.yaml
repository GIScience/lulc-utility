defaults:
  - model: baseline

cache:
  dir: ../cache

data:
  dir: ../data
  descriptor:
    area: v1
    imagery: v1
    labels: v1

hydra:
  run:
    dir: ../outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}

neptune:
  project: ${oc.env:NEPTUNE_PROJECT_ID}
  api_token: ${oc.env:NEPTUNE_PROJECT_API_TOKEN}

sentinel_hub:
  api:
    id: ${oc.env:SENTINELHUB_API_ID}
    secret: ${oc.env:SENTINELHUB_API_SECRET}


